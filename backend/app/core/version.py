"""The application's name and version, in one place.

Both the OpenAPI document and the app's own About screen read from here, so
there is one number to change and nothing to keep in step.

Semantic versioning, and still below 1.0, so:

    MINOR   a new capability — budgets, attachments, a screen that was not
            there before.
    PATCH   everything else: fixes, refinements, and work that changes how
            the app is built rather than what it does.

Bump it in the same commit as the work, and say what changed in CHANGELOG.md.
A number nobody can map back to a change is just a number.
"""

APP_NAME = "Montra"
APP_VERSION = "0.4.15"
